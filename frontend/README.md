# Frontend (Member 6)

Three dashboards, all API calls routed through src/api/client.js -> the
gateway. Never call a backend service URL directly from a component.

## Run locally
```
npm install
npm run dev
```
